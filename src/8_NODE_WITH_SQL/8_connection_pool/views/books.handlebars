<h1>Available books: </h1>
<div class="books-container">
    <a href="/">Insert new book</a>
    {{#each books}}
        <div>
            #{{this.id}} - 
            <span class="bold"><a href="/books/{{this.id}}">{{this.title}}</a></span>
            - {{this.pageqty}} pages
        </div>
    {{/each}}
</div>