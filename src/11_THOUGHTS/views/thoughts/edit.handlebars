<div class="thought-form-container">
    <a href="/thoughts/dashboard">Back</a>
    <h1>Editing thought:</h1>
    <form action="/thoughts/edit" method="POST">
        <input type="hidden" name="id" value="{{thought.id}}">
        <div class="form-control">
            <label for="title">Title:</label>
            <input type="text" name="title" placeholder="What are you thinking?" value="{{thought.title}}">
        </div>
        <input type="submit" value="Edit thought">
    </form>
</div>